services:
  - type: web
    name: logscale-backend
    runtime: node
    rootDir: apps/backend
    buildCommand: npm install && npm run build && npx prisma generate
    startCommand: node dist/src/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
